pandocs
tmp
target
rp235x-hal-examples
rp235x-hal
rp235x-hal-macros
rp-hal-common
rp-binary-info
picotool.exe
check.py
.DS_Store
# config.toml is not local tooling here: it carries the linker flags and the
# picotool runner the Pico 2 target needs, so it has to be tracked.
.cargo/*
!.cargo/config.toml
.claude/
.idea/
Cargo.lock
flamegraph.svg
src/.DS_Store

# Commercial cartridge dumps. Supply your own -- see README.md.
#
# One level only, so the freely redistributable blargg suite under
# test-roms/gb-test-roms-master/ stays tracked; that is what the test suite
# actually needs.
test-roms/*.gb
test-roms/*.gbc